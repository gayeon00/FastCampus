코틀린에 존재하는 특이한 자료형/키워드

1. Unit
- 자바 -> void
- 반환값이 없는 함수를 만들 때 사용하는 자료형

2. Any/Any?
- Any  -> non-null 타입의 조상(Int, Short, ...)
- Any? -> nullable 타입의 조상(Int?, Short?, ...)

3. Nothing
- fun 함수명(변수명: 타입, ...) : Nothing{
} => 이 함수를 호출하면 뭔가 잘못된 것임을 알려주는 함수!!
- 명시적으로 잘못됨을 알려줌! (모든 라인을 읽어보지 않아도 알 수 있음!!)